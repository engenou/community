<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nowcoder.community.mapper.CommentMapper">

    <sql id="selectFields">
        id
        , user_id, entity_type, entity_id, target_id, content, status, create_time
    </sql>

    <sql id="insertFields">
        user_id, entity_type, entity_id, target_id, content, status, create_time
    </sql>

    <sql id="insertSelectiveFields">
        <trim suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="entityType != null">entity_type,</if>
            <if test="entityId != null">entity_id,</if>
            <if test="targetId != null">target_id,</if>
            <if test="content != null">content,</if>
            <if test="status != null">status,</if>
            <if test="createTime != null">create_time</if>
        </trim>
    </sql>

    <sql id="insertSelectiveValues">
        <trim suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="entityType != null">#{entityType},</if>
            <if test="entityId != null">#{entityId},</if>
            <if test="targetId != null">#{targetId},</if>
            <if test="content != null">#{content},</if>
            <if test="status != null">#{status},</if>
            <if test="createTime != null">#{createTime}</if>
        </trim>
    </sql>


    <select id="selectCommentByEntity" resultType="com.nowcoder.community.entity.Comment">
        select <include refid="selectFields"></include>
        from comment
        where status = 0
        and entity_type = #{entityType}
        and entity_id = #{entityId}
        order by create_time asc
        limit #{offset}, #{limit}
    </select>

    <select id="selectCountByEntity" resultType="int">
        select count(id)
        from comment
        where status = 0
        and entity_type = #{entityType}
        and entity_id = #{entityId}
    </select>

<!--  VALUES(#{userId},#{entityType},#{entityId},#{targetId},#{content},#{status}, #{createTime}) -->
    <insert id="insertComment" useGeneratedKeys="true" keyProperty="id" parameterType="com.nowcoder.community.entity.Comment">
        insert into comment(<include refid="insertSelectiveFields"></include>)
        values(<include refid="insertSelectiveValues"></include>)
    </insert>

    <select id="selectCommentById" resultType="com.nowcoder.community.entity.Comment">
        select <include refid="selectFields"></include>
        from comment
        where id = #{id}
    </select>


</mapper>